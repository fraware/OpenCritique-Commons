# Natural matcher-audit session manifests

Drop `MatcherAuditSessionManifest` JSON files here after natural audits run.
This directory is intentionally empty until then — gate #6 reads evidence from
these files and reports natural=0 when none exist.
